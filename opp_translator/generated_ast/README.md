These are for example purposes, and these files are not required for code-generation.

The code-generator will create its own AST (using clang) depending on the available application file once invoked.